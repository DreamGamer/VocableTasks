class Card {
  constructor(id, flag, language, created, lastModified) {
    this.id = id;
    this.flag = flag;
    this.language = language;
    this.created = created;
    this.lastModified = lastModified;
  }
}

export default Card;
